# Config

This folder stores repo-level configuration assets that are not part of the runtime application packages.

- `backend/`: backend-oriented config assets
- `frontend/`: frontend-oriented config assets
- `deployment/`: deployment examples and environment-specific configuration
